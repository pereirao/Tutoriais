import Navigation from './Navigation';

const Header = (props) => {
    return (
        <div>
            <h1>React Router Tutorial</h1>
            <Navigation />
        </div>
    );
}

export default Header;
